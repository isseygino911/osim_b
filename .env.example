PORT=8787

# Optional. When set, POST /api/snapshot requires a matching X-Snapshot-Token
# header — recommended once snapshot pushes run on an automated schedule.
SNAPSHOT_TOKEN=

# Optional. Enables services/newsAI.service.js's Gemini-based news analysis.
GEMINI_API_KEY=
GEMINI_MODEL=
